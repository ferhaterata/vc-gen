program sum
  pre 0 <= N
  post sum <= N * max
is
  sum := 0;
  max := 0;
  i := 0;
  while i < N
    inv i <= N && sum <= i * max
  do
    if max < a[i] then max := a[i]; end
    sum := sum + a[i];
    i := i + 1;
  end
end